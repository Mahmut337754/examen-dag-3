RewriteEngine On

# Already going to /public/ — don't rewrite again
RewriteCond %{REQUEST_URI} ^/public/
RewriteRule ^ - [L]

# Send everything else to /public/index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /public/$1 [L,QSA]
